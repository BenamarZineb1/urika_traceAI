services:
  db:
    image: postgres:15
    container_name: workflow-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 0000
      POSTGRES_DB: workflow_db
    ports:
      - "5432:5432"